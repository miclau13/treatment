{
  "name": "ls",
  "version": "1.0.0",
  "description": "exercise",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart:dev": "yarn install",
    "start:dev": "concurrently \"cd ./frontend && npm start\" \"cd ./backend && npm start\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
