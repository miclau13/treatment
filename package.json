{
  "name": "ls",
  "version": "1.0.0",
  "description": "exercise",
  "main": "index.js",
  "scripts": {
    "build": "cd ./frontend && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart:dev": "yarn install",
    "server": "cd ./backend && npm run server",
    "start:dev": "concurrently \"cd ./frontend && npm start\" \"cd ./backend && npm run server\"",
    "start": "cd ./backend && npm start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.1.0"
  }
}
